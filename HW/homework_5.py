import dudraw

def question_4():
    dudraw.set_canvas_size(600, 500)
    dudraw.clear(dudraw.WHITE)
    dudraw.set_x_scale(0, 600)
    dudraw.set_y_scale(0, 500)

    # Constants
    gr = 25
    rr = 11.75

    gx = 50
    gy = 450

    rxL = 12
    ryL = 450

    rxR = 88
    ryR = 450

    rxT = 50
    ryT = 488

    rxB = 50
    ryB = 412

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

# Row 2
    gy = 375
    ryL = 375
    ryR = 375
    ryT = 413
    ryB = 337

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100

    # Main grey circle 
    dudraw.set_pen_color(dudraw.LIGHT_GRAY)
    dudraw.filled_circle(gx, gy, gr)
    # Four smaller red circles 
    dudraw.set_pen_color(dudraw.RED)
    # Left 
    dudraw.filled_circle(rxL, ryL, rr)
    # Right
    dudraw.filled_circle(rxR, ryR, rr)
    # Top
    dudraw.filled_circle(rxT, ryT, rr)
    # Bottom
    dudraw.filled_circle(rxB, ryB, rr)
    gx += 100
    rxL += 100
    rxR += 100
    rxT += 100
    rxB += 100




    

    dudraw.show()

question_4()